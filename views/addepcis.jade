extends layout

block mainContent
	h2 Adding EPCIS
	
	p
		a(href='/index', class="button small pink") &laquo;  Back to index
	
	if error
		p.error Something went terribly bad: #{error}
	
	form(method='post')
		div(class='listRow')
			label EPCIS to add:
				input(name='epcisname', value='#{epcisname}')
	
		div.formFooter
			input(type='submit', value='Add')
